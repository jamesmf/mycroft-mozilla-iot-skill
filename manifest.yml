dependencies:
  # Pip dependencies on PyPI
  python:
    - requests

  # Require the installation of other skills before installing this skill
  skill:
    - https://github.com/MycroftAI/skill-iot-control
